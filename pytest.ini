[pytest]
markers =
    smoke: fast, critical-path tests — run on every push
    sanity: broader check after a build — confirms key areas work
    regression: full suite — run before a release

addopts = --html=report.html --self-contained-html
headed = false
tracing = retain-on-failure
testpaths = tests